# name: Repository Dispatch

# on:
#   repository_dispatch:
#     types: [new-image]
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v2

#     - name: Update Image Version
#       id: imgupd
#       uses: mikefarah/yq@master
#       with:
#         cmd: yq eval '.spec.template.spec.containers[0].image = "${{ github.event.client_payload.image }}"' -i ./deployment.yaml

#     - uses: stefanzweifel/git-auto-commit-action@v4
#       with:
#         commit_message: Apply image name changes

name: Repository Dispatch

on:
  repository_dispatch:
    types: [new-image]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Update Image Version
      id: imgupd
      uses: mikefarah/yq@master
      with:
        cmd: yq eval '.spec.template.spec.containers[0].image = "${{ github.event.client_payload.image }}"' -i ./deployment.yaml

    - name: Commit and Push Changes
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git add .
        git commit -m "Apply image name changes"
        git remote add origin_with_token https://${{ secrets.PAT }}@github.com/JaimeHenaoChallange/GitOps-Kantox.git
        git push origin_with_token HEAD:${{ github.ref }}
      env:
        YOUR_GITHUB_TOKEN: ${{ secrets.PAT }}

